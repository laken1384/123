#include <stdio.h>
#include<stdlib.h>
int main(void)
{
	int a,b, i,j,x;
	do
	{
		printf("Enter length (-1 to end) : "); scanf_s("%d", &a);
		if (x != -1)
		{
			printf("Enter breadth  : "); scanf_s("%d", &b);
			for (i = 0; i < a; i++) printf("+");
			for (i = 0; i < b - 2; i++) printf("+");
			{
				printf("+");
				for (j = 0; j < a - 2; i++) printf(" ");
				printf("+");
			}
			for (i = 0; i < a; i++) printf("+");
		}
	}while (x==0)
	system("pause");
	return 0;
}